# Skill Registry — nexus-ai-gateway

Generated by `sdd-init` on 2026-04-28. Re-run `skill-registry` skill to refresh.

This registry indexes user-level skills available to this project plus any project-level conventions. SDD skills (`sdd-*`), `_shared`, and `skill-registry` are infrastructure and intentionally omitted.

## Project Conventions

- `~/.claude/CLAUDE.md` — Global user instructions (Agent Teams Orchestrator, SDD workflow, delegation rules). Applies to ALL projects, including this one.
- No project-level `CLAUDE.md`, `AGENTS.md`, `agents.md`, `GEMINI.md`, `.cursorrules`, or `copilot-instructions.md` present in repo root.

## User Skills

Source: `C:/Users/ramse/.claude/skills/`

### Documentation / research

| Skill | Trigger summary |
|-------|-----------------|
| `context7` | Fetch up-to-date docs for any library before writing code. Triggers on library/API/framework questions or "use context7", "busca docs", "documentation for". |

### Frontend / UI (low relevance for this Bun gateway, kept for completeness)

| Skill | Trigger summary |
|-------|-----------------|
| `frontend-design` | Build distinctive production-grade frontend interfaces. Triggers on web components, pages, dashboards, HTML/CSS layouts. |
| `web-design-guidelines` | Review UI code for Web Interface Guidelines compliance. Triggers on "review my UI", "check accessibility", "audit design". |
| `react-best-practices` (registered as `vercel-react-best-practices`) | React/Next.js performance optimization (Vercel Engineering). |
| `next-best-practices` | Next.js best practices — file conventions, RSC boundaries, data patterns. `user-invocable: false`. |
| `next-upgrade` | Upgrade Next.js to the latest version using official migration guides and codemods. |

### Skill maintenance

| Skill | Trigger summary |
|-------|-----------------|
| `ui-skills` | Directory of UI references; no `SKILL.md` (not directly invocable). |

## SDD Skills (omitted from active registry — infrastructure)

`sdd-init`, `sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`, `skill-registry`, `_shared`.

## Project-Relevant Skill Pickers

For day-to-day work on `nexus-ai-gateway` (Bun + TypeScript backend), the most useful skills here are:

1. `context7` — fetch fresh docs for `@libsql/client`, `pino`, `prom-client`, `openai` SDK, etc., before generating code.
2. `sdd-*` family — drive feature work via `/sdd-new`, `/sdd-explore`, etc.

Frontend/Next.js skills are bundled with the user profile but rarely apply to this gateway's server-side surface. The dashboard (`dashboard.html`, `test_chat.html`) is plain HTML — `frontend-design` and `web-design-guidelines` may apply when iterating on those.
